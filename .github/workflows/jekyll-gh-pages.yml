# Disabled: This Jekyll workflow was overwriting the Vite build.
# Use deploy.yml instead for Vite-based builds.

name: DISABLED-Jekyll

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use deploy.yml for deployments."
